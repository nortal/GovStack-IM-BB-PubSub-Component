package com.govstack.information_mediator.pubsub.domain.entity.id;

import java.util.UUID;

public class EventTypeVersionID extends EntityID {
    public EventTypeVersionID(UUID id) {
        super(id);
    }
}
